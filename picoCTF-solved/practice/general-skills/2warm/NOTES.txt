da migliorare sicuramente l'algoritmo per stampare il numero
puoi usare un ciclo inverso!

for (int j = size -1, j >= 0; j--)
  dove i e' l'index per posizionare le cifre nell'array
  e' insomma l'offset
